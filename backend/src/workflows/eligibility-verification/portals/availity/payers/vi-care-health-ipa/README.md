# VI Care Health IPA

VI Care Health IPA is an input payer alias for the existing Van Lang IPA Availity eligibility payer.

This folder intentionally contains no workflow implementation. VI Care Health IPA rows are
routed to `van-lang-ipa` so they reuse the Van Lang IPA workflow without duplicating code.