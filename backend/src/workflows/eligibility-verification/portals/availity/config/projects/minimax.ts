import type { AvailityProjectConfig } from "./types";

/** No overrides: retain the existing Minimax payer workflows and selectors. */
export const minimaxAvailityConfig: AvailityProjectConfig = { id: "minimax" };
